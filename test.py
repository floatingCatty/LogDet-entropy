from utils import plotByLayer, plotByEpoch, plotList, saveCaL_rateDistortion, saveIB
import matplotlib.pyplot as plt
import torch
from model import ResNet, resnet18, FNN
import cupy as cp
import seaborn as sns
import numpy as np





if __name__ == '__main__':
    # PATH = "log/.pth"
    # PATH = 'log/forpaper/CaL/CaL38_Adam_ReLU.pth'
    # f = torch.load(PATH, map_location='cpu')
    # # name = 'CaL332_mr'
    # # PATH = "log/forpaper/CaL/"+name+".pth"
    # rate_train = f['rate_train']
    # rate_test = f['rate_test']
    #
    # for i in range(4):
    #     plt.plot(rate_train[:,i,0] - rate_train[:,i,3])
    #     plt.show()

    print(len('agh'))

















